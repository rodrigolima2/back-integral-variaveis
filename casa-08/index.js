const PI = 3.14;

let = raio = 10, comprimento = 0, area = 0;

comprimento = 2 * PI * raio;
area = PI * (Math.pow(raio, 2));

console.log(`O comprimento do circulo é de ${comprimento} e a area é de ${area}`);